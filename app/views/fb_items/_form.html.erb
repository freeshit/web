<%= form_for(@fb_item) do |f| %>
  <% if @fb_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fb_item.errors.count, "error") %> prohibited this fb_item from being saved:</h2>

      <ul>
      <% @fb_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
